-- Prueba de comportamiento de funciones.

func add(a: int, b: int) -> int -- El retorno de una función debe ser explícita.
    return $a + $b
end

func multiply(a: int, b: int) -> int
    return $a * $b
end

var result: int = $add(10, 20)

$print($result)
$print($multiply($result, 2))
