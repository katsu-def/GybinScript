# ALL:
# Paquete principal de GBN.
# `Parser.py` contiene el entrypoint de consola.
# `engine.py` contiene el motor de ejecucion del lenguaje.
# `runtime.py` define configuracion compartida, tipos y rutas.
# `native_io.py` aporta built-ins y lectura de archivos.
# `source_tools.py` agrupa parsing textual y limpieza de comentarios.
# `compiler.py` encapsula PyInstaller y el wrapper fallback.
